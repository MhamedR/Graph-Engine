# graph-engine-reactive

Push/pull reactive runtime: values, computeds, effects, batching, and scheduling.

This package is private. Consumers should import it from the published `graph-engine` package.
