# graph-engine-graph

Directed graph primitives (`DirectedGraph`, `Node`, `GraphEdge`) and the traversal and analysis algorithms published through `graph-engine/graph`.

This package is private. Consumers should import it from the published `graph-engine` package.
