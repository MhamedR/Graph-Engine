# graph-engine

Published npm package for this repository. It re-exports the graph and reactive public APIs:

```ts
import {DirectedGraph, ReactiveRuntime} from 'graph-engine';
import {DirectedGraph} from 'graph-engine/graph';
import {ReactiveRuntime} from 'graph-engine/reactive';
```

See the repository root README for usage, algorithms, and the reactive contract.
