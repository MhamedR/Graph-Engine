---
---

Ship the first `graphora` release at 1.0.0. This package has never been published, so the version must not increase.

Add a runtime plugin contract (`runtime.use()`), computation events for computed evaluations and effect runs, and the `graphora/store`, `graphora/inspector`, `graphora/devtools`, and `graphora/opentelemetry` subpaths.
