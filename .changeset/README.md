# Changesets

Run `npm run changeset` for every user-visible change. Commit the generated
Markdown file with the implementation. Release automation consumes changesets
to update package versions and changelogs.
